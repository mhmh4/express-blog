# JSBA

## Quickstart

1\) Install dependencies.

```
npm i
```

2\) Run `prisma migrate`.

```
npx prisma migrate dev --name init
```

3\) Create a `.env` file with a `SECRET_KEY` value.

```
SECRET_KEY="secret_key"
```

4\) Start the server.

```
npm run dev
```
